# .bash_profile

export LD_LIBRARY_PATH=${HOME}/local/lib:${LD_LIBRARY_PATH}
export PATH=${HOME}/local/bin:${PATH}

# if [[ -n "$TMUX" ]] && [[ -n "$ZSH_VERSION"=="5.8" ]]; then
#   export SHELL=${HOME}/local/bin/zsh
#   exec $SHELL -l
# else
#   ${HOME}/local/bin/tmux 
# fi

# if [ -z "$TMUX" ]; then
#   ${HOME}/local/bin/tmux 
# fi
